
* Validate all user input, command line, file, and window.
* Finish validation of seed value (non negative for command line and .config, not int.MaxValue for window).
* Improve stop speed.
* Apply GA to the application itself for parameter tunning.
* Make new interface to run multiple applications with different parameters.
* Move FitnessFunction to Individual.
